# CueKB 开发任务板

版本：v0.1。当前阶段：M1开发。编码授权：**已于2026-09-09确认**。

## 已完成

- [x] 确定工作名CueKB与中文名线索知识库。
- [x] 建立项目文档目录和设计提案。
- [x] 明确API优先、检索路径、权威数据、发布协议、指标和授权边界。
- [x] 在用户指定目录创建项目并初始化本地Git。
- [x] 创建FastAPI、领域对象、端口和内存开发适配器。
- [x] 实现文本开发入口、结构化分段、关键词检索、RRF和显式降级状态。
- [x] 建立PostgreSQL初始DDL与容器配置。
- [x] 安装基础开发依赖并通过4项核心/API测试。
- [x] 初始化本地Git并将当前开发基线发布到GitHub `ttonghao-thao/CueKB`。

## 待确认后执行

| 阶段 | 工作 | 交付与完成标准 | 依赖 |
| --- | --- | --- | --- |
| M0 基线（进行中） | 设计与工程已确认；基础依赖、环境配置和健康检查已建立；仍需锁定解析/模型版本并记录目标硬件 | 当前4项测试通过；Docker未在执行环境安装，Compose尚未实跑 | 用户确认已完成 |
| M1 文档与关键词（进行中） | 内存纵切和初始DDL已完成；继续实现PG适配器、API Key、文件上传、任务租约、Docling、原文定位、版本发布和OpenSearch BM25 | PDF/DOCX/MD/TXT样本可检索定位；权限、幂等、解析失败与版本切换验证通过 | M0 |
| M2 混合检索 | Embedding适配器、向量索引、并行召回、RRF、重排、超时预算、降级状态 | 关键词/向量/混合/重排消融结果；阶段延迟和输入长度可观测；模型版本不混用 | M1 |
| M3 轻量关联与更新 | 实体/别名/关系/证据、一跳扩展、上下文预算、增量、撤权、删除和恢复演练 | 关系有出处、扩展有上限、旧版本/撤权内容不返回；故障重试不重复发布 | M2 |
| M4 质量与性能 | 约200条标注问题、冻结测试集、无答案阈值校准、负载测试、备份恢复、发布验收 | 实测报告写明配置；目标未达时解释瓶颈和调整；不通过强制门禁不得宣称生产就绪 | M3 |
| M5 门户与可选回答 | 轻量搜索/导入/原文/版本页面；按需LLM回答与引用 | 仅消费已验收API；清楚显示冲突、未知和降级；不把生成时间算作搜索时间 | M4及对应范围授权 |

M0—M4为拟议首期。M5是后续范围，用户本次也可要求提前纳入。设计批准后，M0—M4无需每阶段重复确认。

## 首批必要验证

- 相同文件和幂等键重复提交不产生重复发布。
- 解析/OCR失败不会发布空内容；低质量内容不会被当作正常已发布资料。
- 标题/错误码/型号精确字段与中文行业别名能够正确匹配。
- 旧版本与新版本切换后，单次结果不混用同文档的两个可见版本。
- 撤权/删除后，缓存、索引、关系扩展和原文下载都不返回失效内容。
- 生成向量的模型与查询模型不一致时拒绝该向量路径或显式降级。
- 重排/Embedding故障时返回已有合法结果并标记降级，权限服务失败时拒绝访问。
- 多证据问题按所需证据集合验收，不能仅用“前5有一个命中”替代。

## 当前检查点

- 已有产物：设计文档、Python工程、开发API纵切、初始DDL、Compose、测试与本地虚拟环境。
- 仓库状态：当前开发基线已发布到GitHub `ttonghao-thao/CueKB` 的 `main` 分支。
- 未做事项：生产持久化、正式文件上传、Docling、OpenSearch/BGE/重排、权限与性能实测。
- 下一动作：实现PostgreSQL仓储、迁移与API Key知识库权限，再接入文件上传Worker。
- 环境缺口：真实样本文档、目标主机/目录、硬件配置与期望QPS；可在M0补齐，不伪造默认资源事实。
