# CueKB 架构决策

状态：v0.1方案已于2026-09-09获用户确认；M0、M1、M2编码已完成，后续编码状态以任务板为准。

| 编号 | 决策 | 原因与边界 |
| --- | --- | --- |
| D01 | 名称CueKB，中文线索知识库，目录cuekb | 表达由线索找到知识；工作名未核验商标 |
| D02 | 检索优先，回答生成独立 | 缩短证据返回时间，便于客服Agent复用 |
| D03 | PostgreSQL＋OpenSearch | 事务性权威数据与搜索投影职责清晰；接受两者最终一致，但最终返回必须校验 |
| D04 | BM25＋稠密向量＋RRF＋有限重排 | 兼顾术语、口语与问题相关性；通过消融评测验证收益 |
| D05 | BGE-M3与bge-reranker-v2-m3作为质量基线 | 公开可复现的多语言基线，不宣称最新最强或CPU必达标 |
| D06 | 首期图谱存PostgreSQL，限定一跳 | 支持确定性上下文和关系扩展，控制噪声与成本 |
| D07 | 独立Worker＋数据库任务和outbox事件 | 不引入额外消息服务；以租约、重试和幂等处理任务 |
| D08 | 首期知识库级ACL | 边界容易验证；不同可见范围拆知识库，暂不声称支持文档级ACL |
| D09 | 首期API和评测闭环；门户后置 | 2026-09-15用户确认启动首期门户后被D13替代 |
| D10 | 原始文件保留、派生索引可重建 | 减少解析/模型升级造成的数据不可恢复风险 |
| D11 | 核心技术栈一致，生产和个人独立部署 | 共享维护成本，不混用数据；模型配置不同仍须各自评测 |
| D12 | 不自动按访问次数修改知识真伪或覆盖原文 | 使用行为仅作诊断与候选排序信号，避免强化错误 |
| D13 | API服务内置首期静态管理门户 | 直接复用现有API Key、ACL和导入/检索接口，不增加独立前端运行时；范围含导入、任务、文档版本、原文、删除和检索验证 |

## 已知假设与默认处理

| 项目 | 处理方式 | 状态 |
| --- | --- | --- |
| 项目目录 | 已在`/Users/snowking/Documents/CueKB`创建工程 | **已完成** |
| 标准库/模型/解析版本 | Python依赖冻结，Embedding和reranker通过明确的 OpenAI-compatible `model`标识调用 | **已完成** |
| 真实语料规模与扫描件比例 | 用真实样本建立基线，再制定容量计划 | 验证活动**待开始** |
| 并发与QPS | 初始建议10万内容块、5 QPS稳态和10 QPS短时负载 | 条件**待确认** |
| 可用模型硬件 | 确定目标主机或独立模型服务的CPU/GPU与内存 | **待确认** |
| Web门户 | 首期管理门户由API服务内置并使用知识库ACL | **已完成** |
| 生产身份系统 | 知识库范围API Key已完成；OIDC适配范围另行确定 | API Key**已完成**；OIDC**待确认** |

## 评审要点

项目名与核心目标、首期范围、主技术栈和API优先交付已确认，无需重复评审。模型可替换但必须重建向量与重新评测。资源和QPS不是阻塞设计确认的条件，但在测量前不得承诺生产达标。

## 后续变更规则

该批决策已标记为accepted。变更须记录日期、原因、影响和被替代决策；不抹去历史理由。正常代码修复不需要新增架构决策。
