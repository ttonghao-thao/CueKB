# CueKB 协作约定

## 按需阅读

- 默认只读本文件、[项目核心](PROJECT_CORE.md)和[任务板](TASK_BOARD.md)；已在上下文中的内容不重复读。
- 按任务从[文档导航](docs/README.md)选择一份专题；先`rg -n '^##' 文件`定位，再读取相关章节，避免通读全部文档。
- 修改前检查Git状态、适用AGENTS、实际调用链与测试；保留用户未提交修改。源码/迁移是实现依据，计划不是现有契约。
- `docs/archive/`仅追溯历史时读取指定文件，禁止默认递归加载；历史授权和计划不自动扩大当前任务范围。

## 不变边界

- PostgreSQL是内容、权限、删除与可见版本的权威，OpenSearch是可重建投影；搜索、关系、上下文及未来缓存均须最终校验。
- CueKB支持多语言证据检索；生成式回答完全由外部系统决定与集成。不新增内置LLM回答或无限图遍历。
- 所有候选、扩展、上下文、超时、重试有界；重试有界退避，权限不足不得放宽授权。
- 相似度/重排分数不等于事实真实概率；未校准状态保留`unassessed`。
- 复用确定性Worker、幂等任务和发布协议。不无依据新增Neo4j、Redis、消息队列、独立向量库或多Agent编排。
- 不擅自使用付费服务或把私有文档送往外部模型。模型变更须保持查询/文档配置一致并重建匹配索引。

## 执行与交接

用户2026-09-09已授权按批准设计编码，无须逐阶段重复确认；最新指令优先。既有授权不自动覆盖待确认扩展；当前任务范围见任务板。

复杂任务先在任务板维护计划，再分阶段实现验证。只为具体风险测试；文档修改做链接、源码契约和差异检查，不把历史测试记成本轮执行。完成时更新任务板的变更、验证、阻塞及下一步。代码完成与生产验收分开，性能数字必须注明条件。

文档每项事实只维护一个主位置：范围在项目核心，进度在任务板，已接受决策在DECISIONS，当前架构在SYSTEM_DESIGN，接口在API_GUIDE，操作在OPERATIONS，未实现方案在plans。提交/推送依当前授权，不自动执行。
