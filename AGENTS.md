# CueKB 编码协作约定

## 当前授权边界

用户已于2026-09-09明确要求在`/Users/snowking/Documents/CueKB`创建项目并开始编码。该确认允许按已批准设计和任务板开展编码与必要验证，无须反复确认每个阶段。最新用户指令优先。

## 最小阅读路径

先读`PROJECT_CORE.md`与`TASK_BOARD.md`，再按当前任务读取`docs/SYSTEM_DESIGN.md`的相关章节和`DECISIONS.md`。避免每次重复加载全部文档。

## 实现原则

- PostgreSQL是内容、权限与可见版本的权威来源；OpenSearch是可重建投影。
- 不在普通检索链路引入生成式LLM或无限制图遍历。
- 所有候选、扩展、上下文、超时与重试必须有上限；重试使用有界退避。
- 搜索/图遍历/缓存必须执行权限、删除和版本校验；结果不足时不得放松权限。
- 不用相似度、重排分数或频率表示事实真实性；未校准的证据状态标为`unassessed`。
- 以确定性任务与幂等操作实现增量更新；遵守设计中的发布协议和最终版本校验。
- 不提前引入Neo4j、独立向量库、Redis、分布式消息队列或多Agent编排，除非测量或新需求证明必要。
- 不擅自使用付费外部服务或把私有文档送到外部模型。
- 只为具体风险建立测试；优先权限/撤权、删除、版本切换、幂等与检索质量。性能结果附运行条件，不编造达标结论。

## 工作与交接

每次完成任务同步任务板，记录已修改内容、实际验证结果、剩余阻塞和下一个可执行动作。若因额度或环境中断，先尽可能写清检查点；恢复后从检查点继续，不重做已验证步骤。维护已有文档，不为同一事实创建多个平行版本。

当前开发命令见`README.md`。生产适配器未完成时，不得将内存纵切称为生产可用版本。提交或远程同步按当时用户授权与所在环境规则执行。
