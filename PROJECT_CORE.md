# CueKB 项目核心

版本：v0.1。状态：ACCEPTED / IMPLEMENTATION_STARTED。更新时间：2026-09-09。

## 目标

为客服Agent和个人检索提供快速、准确、可追溯的证据服务。正确性包括内容相关、型号/版本/时间适用、上下文完整、来源可定位和权限正确。借鉴多线索检索与情境区分，不以复刻人脑为验收目标。

## 两种独立部署

- 生产客服：已发布业务文档、标准API、权限与更新控制、负载下的质量与延迟。
- 个人知识库：本地文件、导出的聊天与项目记录、单用户、可完全离线部署。
- 共用代码、接口与数据结构，不共用数据实例；业务类型与同义词按知识库配置。

## 首期范围

PDF、DOCX、Markdown、TXT导入；扫描PDF的OCR与质量状态；版本管理；原文及结构化内容保存；关键词与向量混合召回；候选重排；原文定位；轻量有证据关系；知识库级权限；增量更新与删除；评测与可观测性。

首期以API和可复现评测闭环验收。门户与生成式问答属于后续阶段，不阻塞核心检索交付。DOC旧格式、图片问答、复杂图形理解、自动获取聊天记录和外部平台同步不在首期。

## 技术基线（待批准）

| 层 | 选择 |
| --- | --- |
| 应用 | Python、FastAPI、Pydantic、SQLAlchemy、Alembic |
| 后台处理 | 独立Python Worker、PostgreSQL任务/事件表 |
| 解析 | Docling；OCR引擎以中文样本验证结果锁定 |
| 权威存储 | PostgreSQL；原始文件走文件存储适配器 |
| 检索 | OpenSearch的BM25与向量索引，RRF融合 |
| 模型 | BGE-M3、bge-reranker-v2-m3作为质量基线 |
| 图谱 | PostgreSQL实体、别名、关系、证据表；限定一跳 |
| 部署 | 本地单机容器配置；生产分离模型、入库和搜索负载 |

版本在M0核对兼容性后固定；不得自动追随`latest`。模型换型必须创建匹配的新索引。

## 关键约束

1. 用户已于2026-09-09确认开始编码；按任务板推进，不逐阶段重复索取同一授权。
2. 搜索返回证据；LLM回答独立，普通检索不调用生成式LLM。
3. 原始资料与已保存正文为事实来源；索引、摘要和模型关系为派生结果。
4. 查询向量与文档向量使用一致的模型修订和编码配置。
5. 权限、删除状态、可见文档版本由PostgreSQL最终校验；缓存也必须校验。
6. 当前版本按产品/项目适用范围确定，不能用上传时间替代业务版本。
7. 所有性能数字都是设计目标，必须绑定语料、QPS、硬件、缓存和模型配置。
8. 模型得分不等于事实真实概率；降级、冲突、证据不足如实返回。

## 文档职责

系统设计是实现细节的唯一主文档；决策记录只写选择和原因；任务板只维护进度、验证与阻塞；本文件保持简短。后续调整优先修改原文档，不不断新增相互重复的设计稿。
