# CueKB 项目核心

版本：v0.1。状态：M0 / M1 / M2 / M3及管理门户编码已完成。更新时间：2026-09-21。

当前交付状态：M0工程与版本基线、M1文档与关键词检索、M2混合检索、M3关系与上下文及管理门户编码均已完成，内存开发模式继续保留。M4质量与运行能力待开始；生成式回答待确认。完整清单以`TASK_BOARD.md`为准。

## 目标

为客服Agent和个人检索提供快速、准确、可追溯的证据服务。正确性包括内容相关、型号/版本/时间适用、上下文完整、来源可定位和权限正确。借鉴多线索检索与情境区分，不以复刻人脑为验收目标。

## 两种独立部署

- 生产客服：已发布业务文档、标准API、权限与更新控制、负载下的质量与延迟。
- 个人知识库：本地文件、导出的聊天与项目记录、单用户、可完全离线部署。
- 共用代码、接口与数据结构，不共用数据实例；业务类型与同义词按知识库配置。

## 首期范围

已完成范围：PDF、DOCX、Markdown、TXT导入；扫描PDF的OCR；基础质量门禁；版本管理；原文、检索块及来源锚点保存；关键词与向量混合召回；候选重排；原文定位；知识库级权限；增量更新与删除；文档导入、任务进度、版本发布、原文下载、删除和检索验证管理门户；有出处关系与审核别名、有界一跳检索、章节上下文、事务快照、索引generation重建及切换回退。

待开始范围：证据状态评估、质量评测工具和运行指标。待确认范围：生成式回答及其他后续集成。

首期以API、管理门户和可复现评测闭环验收。生成式问答属于后续阶段，不阻塞核心检索交付。DOC旧格式、图片问答、复杂图形理解、自动获取聊天记录和外部平台同步不在首期。

## 技术方向与编码状态

| 层 | 选择 | 编码状态 |
| --- | --- | --- |
| 应用 | Python、FastAPI、Pydantic、SQLAlchemy、Alembic | **已完成** |
| 管理门户 | FastAPI静态单页；API Key、导入、任务、版本、原文、删除、检索验证和系统管理员模型配置 | **已完成** |
| 后台处理 | 独立Python Worker、PostgreSQL任务/事件表 | **已完成** |
| 解析 | Docling与PDF OCR | **已完成** |
| 权威存储 | PostgreSQL；原始文件走文件存储适配器 | **已完成** |
| 检索 | OpenSearch的BM25与向量索引，RRF融合 | **已完成** |
| 模型 | Embedding通过门户配置的 OpenAI-compatible API调用；Reranker通过独立地址按需启用，未配置Embedding仍可启动 | **已完成** |
| 轻量关系 | PostgreSQL实体、审核别名、关系与支持/反驳证据；限定一跳 | **已完成** |
| 上下文与一致性 | 章节/相邻块/表头与预算，事务快照与返回前复验 | **已完成** |
| 索引generation | 分页重建、验证、模型与索引事务切换、旧模型重建回退 | **已完成** |
| 部署 | 手动`docker build`制作API/Worker镜像；按角色安装依赖并使用精简runtime层，Worker使用CPU版PyTorch；单机Compose引用本地镜像，API/Worker与外部模型API分离 | **已完成** |

Python依赖已写入`uv.lock`，Embedding与Reranker通过明确的 OpenAI-compatible `model`标识调用，不自动追随`latest`。Embedding模型换型必须创建匹配的新索引。

## 关键约束

1. 用户已于2026-09-09确认开始编码；按任务板推进，不逐阶段重复索取同一授权。
2. 搜索返回证据；LLM回答独立，普通检索不调用生成式LLM。
3. 原始资料与已保存正文为事实来源；索引、摘要和模型关系为派生结果。
4. 查询向量与文档向量使用一致的模型修订和编码配置。
5. 权限、删除状态、可见文档版本由PostgreSQL最终校验；缓存也必须校验。
6. 当前版本按产品/项目适用范围确定，不能用上传时间替代业务版本。
7. 所有性能数字都是设计目标，必须绑定语料、QPS、硬件、缓存和模型配置。
8. 模型得分不等于事实真实概率；降级、冲突、证据不足如实返回。

## 文档职责

README只做按角色阅读入口与启动说明；系统设计是内部架构、组件职责、当前/目标数据流程与实现细节的唯一主文档；API接入说明面向第三方，仅维护当前检索/导入契约及必要前置步骤；决策记录只写选择和原因；任务板分别维护编码状态与验证活动；本文件保持简短。变更接口或组件时同步对应文档，不把目标协议写成已实现契约，不为同一事实维护平行文档。
