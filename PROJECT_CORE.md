# CueKB 项目核心

更新：2026-09-22。状态及验证记录只在[任务板](TASK_BOARD.md)维护。

## 产品与范围

CueKB（线索知识库）为客服Agent和其他系统提供快速、准确、可追溯的原文证据检索，支持中文、英语等多语言文档与查询；各语种质量仍需实测。生产客服与个人知识库共享代码、独立部署和数据；离线模式要求模型服务及解析资源均在本地具备。

输入是查询、显式适用范围和可信身份，输出是证据、锚点、版本及检索状态。CueKB不提供内置生成式回答、回答API或门户问答；外部系统自主决定是否调用自己的LLM。门户用于知识库管理与检索验证。

首期覆盖PDF/DOCX/Markdown/TXT、OCR、版本发布、知识库ACL、混合检索与可选重排、有出处的一跳关系、章节上下文、索引generation和管理门户。DOC旧格式、复杂图形理解、图片/视频语义问答、自动抓取聊天及故障操作执行不在首期。

## 已确认架构

- Python/FastAPI；生产PostgreSQL权威仓储、OpenSearch搜索投影、本地原件适配器、独立Worker和PG任务/outbox。
- 外部Embedding必配、Reranker可选；管理员在门户配置`base_url`、`api_key`、`model`。生成模型不参与普通检索。
- 当前只支持知识库级ACL与每文档唯一`default`发布版本；权限、删除、版本返回前以PG复验。
- 模型/维度与索引保持一致；generation重建不重新解析或任意更换分块算法，当前为`structured-v1`。
- 手动构建API/Worker镜像，Compose引用镜像；内存模式仅开发用，不代表生产行为或生产验收。

## 判断标准

相关性、型号/版本适用性、完整上下文、可定位出处与权限正确共同决定质量。未校准证据标为`unassessed`，模型分数不是真实概率。性能目标依赖真实语料、QPS、硬件、模型及缓存条件，不能用本地测试推断生产达标。

稳定选择及原因见[决策记录](DECISIONS.md)；按任务阅读[文档导航](docs/README.md)。
